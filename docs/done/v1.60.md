# v1.60 完成记录

## 已完成
1. 新增迁移脚本 `sql/migrations/V60__refactor_overhaul_task_and_drop_item.sql`：
   - `sys_overhaul_task` 增加业务字段：`report_unit/accept_user/supervision_user/rescue_users/fault_phenomenon/fault_reason/rescue_situation`
   - 删除旧字段：`fix_user/fix_time/remark`
   - 删除表：`sys_overhaul_item`
2. 后端检修改为主单模式：
   - `RepairTaskController` 移除 item 依赖，列表/新增/编辑/删除仅基于 `sys_overhaul_task`
   - 更新 `RepairTask`、`RepairTaskSaveDto`、`RepairTaskViewDto` 字段
   - 删除 overhaul item 相关后端文件（controller/dto/entity/mapper/service）
3. 前端改造：
   - `OverhaulRecord.vue` 改为主单 CRUD，不再关联配电箱
   - `BoxDetail.vue` 删除检修 tab、检修弹窗和检修请求链路

## 验证
- frontend: `npm run build`
- backend: `mvn -DskipTests compile`

## 回滚
- 回滚本次提交并恢复 `sys_overhaul_item` 相关代码与表结构。