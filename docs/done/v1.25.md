# v1.25 完成记录

## 本次改动（演示模式）
1. 后端临时放开鉴权：所有请求 `permitAll`，避免登录/权限异常阻塞演示。
2. 前端路由守卫临时放开：不再强制登录与角色拦截，直接可进入系统页面。

## 修改文件
- `backend/src/main/java/com/distributionbox/security/SecurityConfig.java`
- `frontend/src/router/index.ts`
- `docs/demands/v1.25.md`
- `docs/done/v1.25.md`

## 为什么这么改
- 用户当前目标是“尽快演示功能”，优先可用性。
- 改动集中在鉴权入口，后续可快速回滚。

## 回滚建议
- 恢复 `SecurityConfig` 中管理员限制与 `authenticated()` 规则。
- 恢复 `router/index.ts` 登录/角色守卫逻辑。
