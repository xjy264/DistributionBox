# v1.71 完成记录

## 根因处理
为兼容不同调用参数与输入形态，后端对台账号检索关键词做统一归一化，前端补充兼容参数传递。

## 修复
- 后端 `BoxController /box/page`：
  - 新增兼容参数 `boxAccount`
  - 对 `boxId/boxAccount` 做 `trim` 归一化后统一用于 `box_id LIKE` 模糊查询
- 前端 `Box.vue`：
  - 查询时对 `filters.boxId` 做 `trim`
  - 同时传 `boxId` 与 `boxAccount` 兼容后端

## 结果
台账号模糊检索恢复可用。