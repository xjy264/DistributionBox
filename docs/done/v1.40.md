# v1.40 完成记录

## 已完成
1. **详情页语义改名**
   - 巡检明细历史 -> 维保记录
   - 抢修记录 -> 检修记录
   - 新增巡检任务 -> 新增维保任务
   - 新增抢修任务 -> 新增检修任务
2. **详情页接口切换**
   - `/inspection-item/page` -> `/maintenance-item/page`
   - `/repair-item/page` -> `/overhaul-item/page`
   - `/inspection-task/*` -> `/maintenance-task/*`
   - `/repair-task/*` -> `/overhaul-task/*`
3. **修复新增无响应**
   - `openInspectionDialog/openRepairDialog` 增加 `try/catch`。
   - `saveInspection/saveRepair` 增加 `try/catch` + 成功/失败消息提示。
   - 保存成功后 `await load()` 刷新列表。
4. **后端兼容改造**
   - 控制器同时支持新旧路径：
     - InspectionTaskController: `/maintenance-task` + `/inspection-task`
     - RepairTaskController: `/overhaul-task` + `/repair-task`
     - InspectionItemController: `/maintenance-item` + `/inspection-item`
     - RepairItemController: `/overhaul-item` + `/repair-item`
5. **数据库兼容迁移**
   - 新增 `V40__add_maintenance_overhaul_compat_views.sql`
   - 增加语义视图：`sys_maintenance_task/item`、`sys_overhaul_task/item`

## 说明
- 旧表与旧接口未删除，保证历史调用不受影响。
- 新前端优先使用 maintenance/overhaul 语义路径。
