# v1.92 完成记录

## 已完成
1. `BoxDetail.vue` 增加 `getCurrentBoxId()`，优先读取 `route.params.id`，失败再兜底 `box.id`。
2. 检修记录新增弹窗打开与保存都改为使用 `currentBoxId` 作为 `boxId` 提交。
3. 当 `currentBoxId` 无效时，新增/保存会明确提示错误并中止，不再静默失败。
4. 检修记录列表加载使用 `currentBoxId` 过滤，确保只展示当前配电箱数据。

## 涉及文件
- `frontend/src/views/BoxDetail.vue`
- `docs/demands/v1.92.md`
- `docs/done/v1.92.md`