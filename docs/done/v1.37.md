# v1.37 完成记录

## 已完成
在小程序项目 `WeChatProjects/DistributionBox` 完成首版移动端接入，后端复用配电箱管理系统。

### 1) 基础架构
- 重置 `miniprogram/app.json` 页面路由为业务页
- 新增 `utils/request.ts` 统一请求封装（自动注入 `Authorization` + `token`）
- 新增 API 模块：
  - `api/auth.ts`
  - `api/box.ts`
  - `api/inspection.ts`
  - `api/repair.ts`

### 2) 页面实现
- 登录页：`pages/login/*`（账号密码登录，成功后存 token）
- 配电箱列表：`pages/box-list/*`
- 配电箱详情：`pages/box-detail/*`（含图片预览）
- 巡检记录列表/新增：`pages/inspection-list/*`、`pages/inspection-form/*`
- 抢修记录列表/新增：`pages/repair-list/*`、`pages/repair-form/*`
- 我的：`pages/me/*`（展示用户并可退出）

### 3) 数据库链路
- 小程序所有业务均通过后端接口访问数据库。
- 不直连数据库，满足双端共库设计。

## 运行说明
1. 微信开发者工具打开项目：`/Users/xuejiayao/WeChatProjects/DistributionBox`
2. 关闭“校验合法域名/HTTPS证书”（本地调试场景）
3. 确保后端运行在 `127.0.0.1:8888`
4. 使用现有账号登录（如 admin）

## 注意
- 当前按你的要求跳过 openid 登录。
- 小程序“全部功能”中的复杂模块（如图片上传、打印、更细粒度编辑）可在 v1.38 继续补齐。
