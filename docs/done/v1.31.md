# v1.31 完成记录

## 修复内容
1. 将后端上传目录默认值从相对路径 `../files/` 改为绝对路径：
   - `/Users/xuejiayao/project/distributionBox/files`
2. 保持上传时目录自动创建逻辑，确保首次上传也能成功写入。

## 修改文件
- `backend/src/main/resources/application.yml`
- `docs/demands/v1.31.md`
- `docs/done/v1.31.md`

## 结果
- 上传文件会落到项目根目录下 `files` 文件夹。
- 避免了 Tomcat 临时工作目录下相对路径导致的 `FileNotFoundException`。
