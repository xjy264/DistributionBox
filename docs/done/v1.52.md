# v1.52 完成记录

## 已完成
1. 数据库迁移脚本：`V52__refactor_maintenance_order_modules.sql`
   - 维保主单新增字段：`maintenance_type`、`maintenance_location`、`maintenance_content`、`maintenance_summary`
   - 新增3张模块表：
     - `sys_maintenance_disease`
     - `sys_maintenance_compare_image`
     - `sys_maintenance_process_image`
   - 删除旧表：`sys_maintenance_item`
2. 后端重构
   - `InspectionTaskController` 改为主单保存/查询，不再依赖 item。
   - 新增 `MaintenanceModuleController` 提供3个模块的增删改查接口。
   - 新增实体/Mapper：Disease、CompareImage、ProcessImage。
3. 前端重构
   - `MaintenanceTaskDetail.vue` 改为4个Tab模块化编辑。
   - `MaintenanceRecord.vue` 查询与列表改为主单维度，支持维保类型筛选。
   - `BoxDetail.vue` 移除维保信息Tab。

## 说明
- 维保工单与配电箱关系已解绑，改为纯主单模型。
