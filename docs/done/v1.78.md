# v1.78 完成记录

## 已完成
- 新增字段并接入前后端：
  - `sharedWithOthers`（是否与其它单位共用）
  - `sharedScope`（共用范围）
  - `highPowerAppliance`（是否为大功率电器）
  - `highPowerName`（大功率电器名称）
- `BoxDetail.vue` 与 `Box.vue` 中将两组字段放置在“系统图”前。
- 实现联动与校验：
  - 共用=否 -> 共用范围清空且禁用；共用=是 -> 保存前校验必填。
  - 大功率=否 -> 名称清空且禁用；大功率=是 -> 保存前校验必填。
- 新增并执行本地数据库迁移 SQL。

## 文件
- `frontend/src/views/Box.vue`
- `frontend/src/views/BoxDetail.vue`
- `backend/src/main/java/com/distributionbox/entity/Box.java`
- `sql/migrations/V78__add_box_share_and_high_power_fields.sql`
- `docs/demands/v1.78.md`
- `docs/done/v1.78.md`