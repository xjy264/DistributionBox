# v1.83 完成记录

## 根因与修复
- 修复新增弹窗 `Box.vue`：避免清空对象键导致提交字段不稳定，改为标准初始化 `initForm()`。
- 新增保存改为显式 `payload`，确保四个字段稳定提交：
  - `sharedWithOthers`
  - `sharedScope`
  - `highPowerAppliance`
  - `highPowerName`
- 联动兜底：
  - 非“是”时强制提交空 `sharedScope/highPowerName`。
- 新增与修改都补充 `try/catch` + 错误提示，避免“点击保存无反馈”。

## 涉及文件
- `frontend/src/views/Box.vue`
- `frontend/src/views/BoxDetail.vue`
- `docs/demands/v1.83.md`
- `docs/done/v1.83.md`