# v1.51 完成记录

## 已完成
1. 维保时间筛选改为可选 datetimerange（支持清空）。
2. 关联台账号显示修复：
   - 默认显示前2个，更多显示 `...`
   - tooltip 悬浮显示全部
3. 增加兜底逻辑：
   - 优先使用后端 `boxAccounts`
   - 若为空，前端通过 `boxIds -> 台账号` 映射补齐，确保两个台账号也能显示。

## 变更文件
- `frontend/src/views/MaintenanceRecord.vue`
- `docs/demands/v1.51.md`
- `docs/done/v1.51.md`
