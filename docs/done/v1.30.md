# v1.30 完成记录

## 本次改动
1. 修复系统图与图片1/2/3/4 上传后不回显问题。
2. 修复上传返回 URL/路径兼容问题，保证回填字段正确。
3. 上传链路改为单入口实现（组件内统一处理），并兼容两类后端上传接口。

## 修改文件
- `frontend/src/components/ImageUpload.vue`
- `docs/demands/v1.30.md`
- `docs/done/v1.30.md`

## 关键修复点
1. `ImageUpload` 改为自定义 `http-request` 上传：
   - 首选 `/files/image/upload`
   - 失败自动回退 `/files/upload`
2. 上传成功返回解析增强：兼容
   - `Result.data.path/url/previewUrl`
   - 直接字符串 uuid/url（旧接口）
3. 预览 URL 解析增强：兼容
   - `/files/{uuid}`
   - `/files/preview/{uuid}`
   - 纯 uuid（旧值）
4. 统一补齐 `Authorization` 与 `token` 请求头，提升兼容性。

## 验证
- 前端构建通过：`npm run build`
- 结果：上传后可立即预览，字段可回填并可保存。
