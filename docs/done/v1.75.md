# v1.75 完成记录

## 根因
生产环境图片请求路径可能为 `/api/files/**`（受 Nginx 转发规则影响），而后端仅放行了 `/files/**`，导致命中鉴权并返回 401。

## 修复
- 后端安全配置 `SecurityConfig` 的 GET 放行规则扩展为：
  - `/files/**`
  - `/files/preview/**`
  - `/api/files/**`
  - `/api/files/preview/**`

## 结果
无论生产环境是否保留 `/api` 前缀，图片资源请求都可匿名访问，不再 401。