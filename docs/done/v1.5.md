# DistributionBox — 执行结果归档（v1.5）

- 对应需求：`docs/demands/v1.5.md`
- 优先级：P1
- 执行时间：2026-02-11
- 状态：已完成

## 根因定位
- 元器件列表接口返回结构不一致：
  - `/components/getAll` 返回原始数组 `List<Components>`
  - `/components/{id}` 返回原始数组 `List<Components>`
- 前端统一按 `res.data.data` 读取，导致保存后刷新时取到 `undefined`，页面不回显。

## 代码改动（最小化）
- `frontend/src/views/Components.vue`
  - 列表赋值改为兼容原始数组与 `Result` 包装：优先取 `res.data.data`，否则取 `res.data`。

## 验证
- 本地构建验证：`cd frontend && npm run build` 通过。
- 逻辑验证点：
  - 元器件新增保存后执行 `load()`，可正确解析接口返回并回显记录。

