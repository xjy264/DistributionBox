# v1.99 完成记录

## 已完成
1. 前端 `BoxDetail.vue` 新增统一结果解包与业务码校验 `unwrapResultData`，避免后端返回 `code!=200` 时被当成功。
2. 维保保存 `saveMaintenance` 增加 `try/catch` 与错误提示，后端业务错误可直接反馈给用户。
3. 维保列表加载与删除接口接入业务码校验，避免静默失败。
4. 后端 `BoxMaintenanceController.save` 增加写入影响行校验：
   - 插入/更新影响行数 <= 0 时返回明确失败信息，不再静默成功。

## 改动文件
- `frontend/src/views/BoxDetail.vue`
- `backend/src/main/java/com/distributionbox/controller/BoxMaintenanceController.java`
- `docs/demands/v1.99.md`
- `docs/done/v1.99.md`