# v1.57 完成记录

## 根因
旧后端没有 `/maintenance-task/update`（触发 405）且 `InspectionTaskSaveDto.inspectionTime` 使用 `LocalDateTime` 默认反序列化，不接受带空格时间字符串（`yyyy-MM-dd HH:mm:ss`）。

## 改动
- 文件：`frontend/src/views/MaintenanceTaskDetail.vue`
- 新增 `toBackendDateTime`：将 `inspectionTime` 从 `yyyy-MM-dd HH:mm:ss` 转成 `yyyy-MM-ddTHH:mm:ss`。
- 保存逻辑继续保留：先调 `/maintenance-task/update`，405 时回退 `/maintenance-task/save`。

## 结果
- 405 兼容保留。
- LocalDateTime 解析错误消除，保存链路可用。