# v1.46 完成记录

## 根因
后端启动时读取了 `backend/target/classes/mapper/CheckMapper.xml`，但该模块源码已删除，导致 MyBatis 解析异常并触发应用启动失败。

## 处理
1. 执行 `mvn clean compile` 清理陈旧 `target` 产物并重建。
2. 校验 `backend/target/classes/mapper`，已不包含 `CheckMapper.xml` 等已删除模块映射文件。

## 结果
- 后端编译通过。
- 启动阻塞点（陈旧 Mapper XML）已移除。

## 你现在可执行
- 在 `backend` 目录重新启动服务（IDE 或 `mvn spring-boot:run`）。
