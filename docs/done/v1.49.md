# v1.49 完成记录

## 已完成
1. 维保工单列表新增查询条件：
   - 任务单号（taskNo）
   - 维保时间（inspectionTime，模糊匹配）
   - 配电箱台账号（boxAccount）
2. 维保工单列表“关联配电箱”列从配电箱ID改为显示台账号。
3. 后端 `/maintenance-task/page` 已支持以上三个过滤参数。

## 后端改动
- `InspectionTaskController.page(...)` 增加 `taskNo / inspectionTime / boxAccount` 查询参数。
- `InspectionItemMapper` 增加：
  - `selectTaskIdsByBoxAccount`
  - `selectBoxAccountsByTaskId`
- `InspectionTaskViewDto` 增加 `boxAccounts` 字段。

## 前端改动
- `frontend/src/views/MaintenanceRecord.vue`
  - 新增三个查询输入
  - 请求参数同步传递
  - 表格改为显示 `boxAccounts`

## 验证
- frontend `npm run build`：通过
- backend `mvn -DskipTests compile`：通过
