# v1.42 需求文档

## 目标
完成维保/检修数据实体化迁移，不再仅靠映射；迁移后删除旧表，并同步前端侧边栏名称。

## 范围
1. 数据迁移：
   - 旧表 `sys_inspection` + `sys_inspection_box` -> 新表 `sys_maintenance_task` + `sys_maintenance_item`
   - 旧表 `sys_emergency_repair` + `sys_emergency_repair_box` -> 新表 `sys_overhaul_task` + `sys_overhaul_item`
2. 清理旧表：删除四张旧表。
3. 后端适配：实体与 mapper 指向新表。
4. 前端侧边栏改名：
   - 维保 -> 维保记录
   - 抢修 -> 检修记录

## 验收
- 新表有真实迁移数据。
- 旧表已删除。
- 维保/检修相关接口可继续工作。
- 侧边栏显示新名称。
