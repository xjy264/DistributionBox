# v1.44 需求文档

## 目标
同步数据库菜单变更到前端路由与页面，恢复并规范“维保记录/检修记录”侧边栏入口。

## 范围
1. 侧边栏恢复并显示：维保记录、检修记录。
2. 路由恢复：`/maintenance`、`/fix`。
3. 新建页面文件并与新接口语义对齐：
   - `MaintenanceRecord.vue` -> `/maintenance-task/page`
   - `OverhaulRecord.vue` -> `/overhaul-task/page`

## 验收
- 登录后侧边栏可见“维保记录”“检修记录”。
- 页面可正常打开并展示列表数据。
