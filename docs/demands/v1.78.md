# v1.78 需求文档

## 需求
在配电箱信息中新增两组联动字段（放在系统图前）：
1. 是否与其它单位共用（是/否） + 共用范围
2. 是否为大功率电器（是/否） + 大功率电器名称

## 联动规则
- 共用=否：共用范围强制清空并禁用；共用=是：共用范围必填。
- 大功率=否：大功率电器名称强制清空并禁用；大功率=是：大功率电器名称必填。

## 范围
- 详情页编辑（必做）
- 新增页同步
- 后端实体与数据库字段新增
- 本地数据库执行迁移 SQL