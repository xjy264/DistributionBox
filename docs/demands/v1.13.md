# v1.13 需求文档（old-box 端口调整）

## 需求
将 old-box 默认端口调整为：
- 前端：`8001`
- 后端：`8002`

并保证前后端联通：前端 API 请求应指向 `localhost:8002`。

## 期望输出
1. old-box 后端配置默认监听 `8002`
2. old-box 前端 dev server 默认监听 `8001`
3. 前端 `request` 基础地址不再写死 8888，改为可从配置读取（默认 8002）
4. 给出启动验证结果与访问地址
