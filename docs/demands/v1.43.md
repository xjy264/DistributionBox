# v1.43 需求文档

## 目标
精简冗余功能模块与数据库表，保留核心配电箱业务与组织结构（保留 `sys_department`）。

## 删除范围
1. 前端模块/路由/菜单：Course、Check、Fix、Maintenance、Record、Tax。
2. 后端模块：对应 Controller/Service/Mapper/Entity/Mapper XML。
3. 数据库表：`course`、`student_course`、`sys_check`、`sys_fix`、`sys_maintenance`、`sys_record`、`sys_tax`。

## 保留
- `sys_department` 及其模块保留。
- 新维保/检修主链路：`sys_maintenance_task/item`、`sys_overhaul_task/item` 保留。
