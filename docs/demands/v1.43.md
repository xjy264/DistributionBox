# v1.43 需求文档

## 目标
在保留 `sys_department` 的前提下，清理冗余功能模块及其数据库表，精简前后端代码。

## 删除范围
1. 数据库表：
   - `course`
   - `student_course`
   - `sys_check`
   - `sys_fix`
   - `sys_maintenance`
   - `sys_record`
   - `sys_tax`
2. 前端：删除对应页面与路由/菜单入口。
3. 后端：删除对应 controller/entity/service/mapper 及 mapper xml。

## 验收
- 上述 7 张表不存在。
- `sys_department` 保留。
- 前后端构建通过。
