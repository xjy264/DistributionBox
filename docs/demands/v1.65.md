# v1.65 需求文档

## 目标
为前端删除动作统一增加确认提示，并在检修记录列表页支持删除检修工单。

## 范围
- 页面：`frontend/src/views/OverhaulRecord.vue`
- 在检修记录列表操作列增加“删除工单”按钮，调用 `DELETE /overhaul-task/{id}`
- `frontend/src/views`、`frontend/src/components` 内所有 `http.delete` 触发点增加 `ElMessageBox.confirm`
- 用户确认后才执行删除，取消时不执行删除
