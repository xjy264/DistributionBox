# v1.46 需求文档

## 问题
后端启动失败，MyBatis 解析已删除的 `CheckMapper.xml`（位于 `backend/target/classes/mapper`）导致 `sqlSessionFactory` 初始化异常。

## 目标
清理构建产物中的陈旧 mapper 资源，恢复后端可启动状态。

## 方案
1. 执行 `mvn clean compile` 清空 `target` 并重建。
2. 校验 `backend/target/classes/mapper` 不再包含已删除模块的 XML。
3. 重新验证编译通过。
